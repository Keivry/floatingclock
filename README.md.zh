# FloatingClock（悬浮时钟）

一款极简 Android 悬浮时钟 — 以透明叠加层显示 HH:mm:ss，可拖动到任意位置。

## 功能

- 精确到秒的实时时钟（HH:mm:ss），始终显示在其他应用上方
- 透明背景 — 仅时间文字可见
- 长按拖动到屏幕任意位置
- 3 种字体：默认、等宽、衬线
- 7 种颜色：白色、红色、绿色、蓝色、黄色、青色、品红
- 所有设置自动保存

## 使用说明

1. **安装** — 从 [Releases](https://github.com/Keivry/floatingclock/releases) 下载最新 APK 安装。如遇安全提示，请允许浏览器"安装未知应用"。
2. **授权** — 首次开启悬浮窗时，按引导前往系统设置，开启"在其他应用上层显示"权限。
3. **开启** — 在应用中打开开关。屏幕上会出现一个小巧的时间文字。
4. **拖动** — 长按时钟约 0.5 秒后即可拖动到理想位置。
5. **自定义** — 在应用中选择喜欢的字体和颜色。

## 系统要求

- Android 8.0 及以上

## 所需权限

- **在其他应用上层显示** — 用于显示悬浮时钟
- **通知权限** — Android 系统要求后台服务必须显示通知
- **忽略电池优化**（可选）— 防止系统关闭悬浮时钟

## 许可证

MIT
